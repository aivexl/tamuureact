{
    "name": "@tamuu/api",
    "private": true,
    "version": "0.1.0",
    "scripts": {
        "dev": "wrangler dev",
        "deploy": "wrangler deploy",
        "deploy:bg-remover": "wrangler deploy -c wrangler-bg.toml"
    },
    "devDependencies": {
        "wrangler": "^4.0.0"
    }
}