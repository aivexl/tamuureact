{
    "name": "@tamuu/api",
    "private": true,
    "version": "0.1.0",
    "scripts": {
        "dev": "wrangler dev",
        "deploy": "wrangler deploy",
        "deploy:api": "wrangler deploy -c wrangler-api.toml"
    },
    "devDependencies": {
        "@supabase/supabase-js": "^2.89.0",
        "wrangler": "^4.0.0"
    }
}